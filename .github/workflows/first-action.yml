name: First workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest 
    steps:
      - name: Print greating
        run: "Hello world"
      - name: Print goodbye
        run: "Done bye"
